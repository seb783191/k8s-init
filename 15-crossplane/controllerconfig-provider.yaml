apiVersion: pkg.crossplane.io/v1alpha1
kind: ControllerConfig
metadata:
  name: aws-config
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::AWS_ACCOUNTID:role/ENV-eksctl-crossplane-system
spec:
  podSecurityContext:
    fsGroup: 2000
  args:
    - --debug
